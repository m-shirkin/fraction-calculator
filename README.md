Everything you want to know about any fraction on a single page.

To run this:  
- clone this repository
- serve via any http web server, opening html in browser won't load .wasm module

WASM module built with clang + CMake. To build it on Windows run `src/make.bat`, on other systems execute similar commands manually.